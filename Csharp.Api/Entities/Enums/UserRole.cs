namespace Csharp.Api.Enums
{
    /// <summary>Cargos para autenticação de funcionários via claims do JWT.</summary>
    public enum UserRole
    {
        OPERACIONAL,
        ADMINISTRATIVO,
        TEMPORARIO
    }
}
